# 📋 Task Board

Minimal Trello-style task board. Dark theme, drag-and-drop, zero dependencies for static mode.

## Static Mode (localStorage)

Just open `index.html` in a browser.

## Server Mode (shared, file-based)

```bash
npm install && npm start
```

→ http://localhost:3000

## Keyboard Shortcuts

- **N** — New card
- **Esc** — Close modal
